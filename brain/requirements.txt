# Lumen brain server deps (pinned to the versions running on the laptop).
# GPU whisper additionally needs the NVIDIA CUDA + cuDNN runtime libs —
# `pip install nvidia-cublas-cu12 nvidia-cudnn-cu12` or a system CUDA install.
fastapi==0.139.2
starlette==1.3.1
uvicorn==0.51.0
httpx==0.28.1
pydantic==2.13.4
numpy==2.5.1
faster-whisper==1.2.1
ctranslate2==4.8.1
onnxruntime==1.27.0      # required for whisper vad_filter
pillow==12.3.0           # PET_VISION image decode
kokoro-onnx==0.4.7       # optional alt TTS voice (PET_TTS_VOICE)
